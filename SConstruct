# vim: ft=python

SConscript('src/SConscript.model', variant_dir='build', duplicate=0)
SConscript('src/SConscript.controller', variant_dir='build', duplicate=0)
SConscript('src/SConscript.view', variant_dir='build', duplicate=0)
SConscript('test/SConscript.test', variant_dir='build_test', duplicate=0)
