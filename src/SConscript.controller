# vim: ft=python

Import('env')

controllerDir = 'Controller'

sourceTargets = [ 'datamanager.cpp' ]

targets = []
for source in sourceTargets:
  targets.append(controllerDir + '/' + source)

env.Library('Controller',targets)
