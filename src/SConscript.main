# vim: ft=python

Import('env')

envCopy = env.Clone()

envCopy.Append(
                CPPPATH = '../src/',
              )

targets = [ 'main.cpp' ]

program = envCopy.Program(target = '../bin/Tracker',
                          source = targets,
                          LIBS = [ 'QtView', 'Controller', 'Common', 'Model', 'QtView' ],
                          LIBPATH = '.')

Return( 'program' )
